{
  "name": "otak-atik",
  "private": true,
  "version": "0.0.0",
  "description": "Platform belajar coding blok visual untuk sekolah Indonesia — monorepo.",
  "workspaces": [
    "editor",
    "paket/*"
  ],
  "scripts": {
    "predev": "npm run build:pemutar",
    "dev": "npm run dev --workspace=editor",
    "test": "vitest run",
    "build:pemutar": "node pemutar/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.0.0"
  }
}
